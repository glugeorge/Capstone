"""Standard library imports.

This file imports all the standard libraries that are required by our software.
Use ``import import_libraries`` in other modules to avoid potential package import
errors. Add other standard libraries to this file as needed.
"""

import matplotlib.pyplot as plt
import matplotlib.animation as animation
from matplotlib import style
import pyvisa
import numpy as np
import time
import random # for testing purposes